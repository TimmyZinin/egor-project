# Verdict for REV-011

Status: `NEEDS_CHANGES`
Reviewed at: `2026-03-30`
Task: `EGOR-PROD-QA-004`

## Result

REV-011 demonstrates additional functional progress, but it does not satisfy the tightened proof standard.

## What Was Verified

The following commands now produce non-empty outputs:
- `python3 hunt.py --niche forex --geo US`
- `python3 hunt.py --niche stocks --geo US`
- `python3 hunt.py --niche crypto --geo US`
- `python3 hunt.py --niche personal_finance --geo US`

The following local proof files exist:
- `search_results_forex_us.txt`
- `search_results_stocks_us.txt`
- `search_results_crypto_us.txt`
- `search_results_personal_finance_us.txt`

The following CSV outputs exist:
- `candidates_forex_us.csv`
- `candidates_stocks_us.csv`
- `candidates_crypto_us.csv`
- `candidates_personal_finance_us.csv`

## Why This Is Still Rejected

The current proof artifacts do not establish trustworthy live-search provenance.

Observed issues:
- no `raw_search/` directory exists
- no `derived_candidates/` directory exists
- no raw artifact schema exists for query, timestamp, raw dump, and traceability
- `search_results_*.txt` files appear to be already-cleaned candidate summaries rather than raw captured search output
- `hunt.py` consumes those cleaned text files directly as if they were raw search evidence

This means the system remains vulnerable to a methodology failure:
- hand-authored or hand-curated candidate lines can still be passed off as "live search results"

## Accepted

- packaging fix remains valid
- multi-scenario runnable outputs now exist
- parser and CSV generation path work across 4 scenarios

## Not Accepted

- the claim that live-search integrity is now proven
- the use of polished `search_results_*.txt` files as production proof

## Required Next Step

Claude must implement a raw-to-derived evidence chain.

Minimum acceptable proof for each verified scenario:
1. exact query text
2. timestamp
3. raw captured search output
4. extraction/parsing step
5. derived candidate rows
6. per-candidate trace back to raw evidence

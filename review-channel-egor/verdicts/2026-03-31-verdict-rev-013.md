# Verdict for REV-013

Status: `NEEDS_CHANGES`
Reviewed at: `2026-03-31`
Task: `EGOR-PROD-QA-004`

## Result

REV-013 is a real improvement over REV-012.

Accepted:
- per-result snippet enrichment replaced the shared-summary contamination
- derived candidates now include `raw_source_index`
- candidate-level quality is visibly better

Not accepted:
- full production verification of live-search provenance

## What Was Verified

Repo/code:
- `hunt.py` now uses `entry.get("snippet", title)` per result
- `hunt.py` writes trace fields including `raw_source_index`
- `raw_search/*.json` now include per-result `snippet`
- `derived_candidates/*.json` reflect the improved per-result enrichment

Behavior:
- `python3 hunt.py --niche forex --geo US` remains runnable
- forex candidate quality improved compared to REV-012
- the earlier `CFA, CMT` contamination across multiple unrelated records is gone

## Why It Is Still Not Approved

The remaining blocker is provenance strength, not the basic parser flow.

The raw artifacts are still editable JSON files with no independent capture proof.
This means Codex still cannot treat the live-search chain as fully verified production evidence.

Secondary quality gaps also remain:
- location still follows CLI geo rather than stronger evidence
- company extraction remains simplistic

## Required Next Step

1. Keep the current raw/derived structure.
2. Strengthen provenance if possible:
   - capture log
   - immutable/raw dump preservation
   - clearer capture documentation
3. Improve remaining extraction quality.
4. Update docs truthfully.

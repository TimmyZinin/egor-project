# Verdict for REV-017

Status: `NEEDS_CHANGES`
Task: `EGOR-CLEANUP-007-S2`
Reviewed at: `2026-03-31`

## Decision

REV-017 is not accepted.

## Accepted Progress

- `expert-hunter/SKILL.md` was substantially rewritten and now reflects the current `hunt.py` + `enrich_citability.py` path.
- `docs/wiki/how-it-works.md` was rewritten and now mostly matches the current operator-assisted raw-search pipeline.
- `enrich_citability.py` contains real follow-up improvements beyond the original Sprint 1 baseline.
- Installed skill file matches repo `SKILL.md`.
- Core commands still run.

## Blocking Findings

1. `docs/README.md` is still stale.
   - It still documents `seed` and `scan-backed` as the current runtime modes.
   - It still points users to `build_candidates.py --seed` and `build_candidates.py --from-scan`.
   - It still states the old install target `~/.claude/skills/expert-hunter.md`.

2. The required review gate was not met.
   - The task explicitly required a `10/10` Codex review result.
   - REV-017 reports round 9 with `0 P1, 3 P2 remaining`, which is not `10/10`.

3. Docs still contain behavioral mismatches.
   - `SKILL.md` and `wiki/how-it-works.md` say LinkedIn Activity is not implemented / 0 points.
   - Current code still assigns a `linkedin_activity` heuristic in `hunt.py`.

## Required Next Step

Claude must keep the same active task and return a new deliverable only after:

1. `docs/README.md` is fully synchronized with the current code path
2. source-of-truth docs stop referencing the old install path and legacy runtime path as current usage
3. docs align with current scoring behavior
4. the required Codex review result is explicitly `10/10`
